<% if user_signed_in? %>
  <nav class="navbar navbar-dark navbar-expand-lg bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand fs-1 fw-bold bangers-regular" href="/"><%= t('clash_royale_bets') %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- "Hamburger" Part -->
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to t('home'), root_path, class: "nav-link" %></li>
          <% if user_signed_in? && (current_user.role_id == 3 || current_user.role_id == 4) %>
            <li class="nav-item"><%= link_to t('battles'), battles_path,  class: "nav-link" %></li>
          <% end %>
          <li class="nav-item"><%= link_to t('bets'), bets_path,  class: "nav-link" %></li>
          <li class="nav-item"><%= link_to t('profile'), profile_path,  class: "nav-link" %></li>
          <li class="nav-item "><%= link_to t('log_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link text-danger" %></li>
        </ul>
      </div>
    </div>
  </nav>
<% else %>
  <nav class="navbar navbar-dark navbar-expand-lg bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand fs-1 fw-bold bangers-regular" href="/"><%= t('clash_royale_bets') %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- "Hamburger" Part -->
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to t('home'), root_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to t('log_in'), new_user_session_path, class: "nav-link text-success" %></li>
        </ul>
      </div>
    </div>
  </nav>
<% end %>